{
  "routes": [
    {
      "method": "GET",
      "path": "/departements",
      "handler": "departements.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/departements/count",
      "handler": "departements.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/departements/:id",
      "handler": "departements.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/departements",
      "handler": "departements.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/departements/:id",
      "handler": "departements.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/departements/:id",
      "handler": "departements.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
